<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="de.spring.webservices.rest.persistence.repository.LocationRepository" >
      
    <resultMap id="locationResultMap" type="de.spring.webservices.domain.Location">
        <id column="ID" property="id" />
        <result column="DESCRIPTION" property="description" />
        <result column="LEVEL_ID" property="levelId" />
        <result column="PARENT_ID" property="parentId" />
        <result column="GPS" property="point" typeHandler="de.spring.webservices.rest.persistence.handler.PointTypeHandler" />
    </resultMap>
    
    
   <select id="findAll" resultMap="locationResultMap">
		SELECT
			LOCATION.ID,
		    LOCATION.DESCRIPTION,
		    LOC_HIERARCHY.LEVEL_ID,
		    LOCATION.PARENT_ID,
		    LOCATION.GPS
		FROM LOCATION_HIERARCHY LOC_HIERARCHY
		INNER JOIN LOCATION LOCATION ON (LOC_HIERARCHY.CHILD_ID = LOCATION.ID)
		WHERE
		    LOC_HIERARCHY.PARENT_ID = 1
		    AND LOC_HIERARCHY.LEVEL_ID != 0
		    ORDER BY LOC_HIERARCHY.LEVEL_ID ASC, LOCATION.ID ASC;
  </select>
  
  <insert id="insert" parameterType="de.spring.webservices.domain.Location">    
        <selectKey resultType="java.lang.Long" keyProperty="id" order="BEFORE">
            SELECT nextval('SEQ_LOCATION_ID')
        </selectKey>
        
  		INSERT INTO LOCATION (
  			ID,
  			DESCRIPTION,
			PARENT_ID,
			GPS
		)
		VALUES (
			#{id},
			#{description},
			#{parentId}
			#{point, typeHandler=de.spring.webservices.rest.persistence.handler.PointTypeHandler}
		)
  </insert>
  

</mapper>